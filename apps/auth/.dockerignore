# Auth-specific exclusions
# Exclude all assets except those needed for auth
assets/
public/

# Keep only necessary package files
!package.json

# Keep only necessary config files
!next.config.ts
!tsconfig.json

# Keep source code
!src/
!app/
!components/
!lib/
!hooks/
!helpers/
!posts/

# Keep packages directory (relative to app directory)
!../packages/

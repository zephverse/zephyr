[phases.setup]
nixPkgs = ["nodejs", "bun", "openssl"]
workdir = "/app"

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bunx turbo build --filter=@zephyr/web"]

[start]
cmd = "bun run start"
